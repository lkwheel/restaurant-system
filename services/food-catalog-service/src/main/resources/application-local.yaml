# This is the local configuration for the Food Catalog Service.
eureka:
  client:
    service-url:
      # Change default-zone to defaultZone
      defaultZone: http://${EUREKA_SERVER:localhost}:8761/eureka/
      #http://eureka-0.eureka-service.default.svc.cluster.local:8761/eureka/ # For Kubernetes
