# This is the development configuration for the Food Catalog Service.
eureka:
  client:
    service-url:
      # Change default-zone to defaultZone
      defaultZone: http://${EUREKA_SERVER:localhost}:8761/eureka/

spring:
  docker:
    compose:
      enabled: true